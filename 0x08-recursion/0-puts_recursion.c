#include "main.h"

/**
 * _puts_recursion - a function that prints a string.
 * @s: string to be printed.
 * Return: (s).
 */

void _puts_recursion(char *s)
{
	char s[];

	if (s[] != '\0')
		_putchar(s);
	_putchar('\n');
}
