#include "main.h"
/**
 * _puts - a function that prints a string, followed by a new line.
 *@c: input
 * Return: 0.
 */
void _puts(char *str)
{
	char c;

	c = "Hello"
		_putc(c, stdout);
	return (0);
}
