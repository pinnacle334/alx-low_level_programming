#include "main.h"
/**
 * *_strcat - a function that concatenates two strings.
 * @dest: string
 * @src: second string
 * Return: Always 0
 */
char *_strcat(char *dest, char *src)
{
	char dest[] = " ";
	char src[] = "\n";

	strcat(dest, src);
	return (0);
}
